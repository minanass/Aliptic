{% extends 'base.html.twig' %}

{% block title %}Accueil
{% endblock %}

{% block body %}
	<div class="mt-5 pt-5">
			
			<div class="row py-3">
				<div id="home-left-section" class=" col-lg-6 col-md-12 home-sections">
					
						<h1 class="fs_225 align-self-center">Sudoku Kai Chi</h1>
						<h2 class="mb-4 fs_225">Bienvenue</h2>

					Lorem ipsum dolor sit amet, consectetur adipiscing elit. Suspendisse id erat facilisis, consequat elit non, eleifend lorem. Fusce imperdiet ante sed sapien hendrerit dictum. Sed sem nulla, semper a lobortis at, facilisis sit amet libero. Praesent tristique mauris augue, id pharetra mi fermentum in. Maecenas ex risus, dignissim at venenatis sit amet, fringilla ac erat. Etiam auctor sollicitudin tellus, ac consequat neque lacinia sed. Integer ex leo, malesuada ut purus vel, gravida condimentum risus.
			

				{% if app.user %}
					{% if app.user.score == 0 %}
						<div class="d-flex justify-content-center mt-5">
							<a href="{{ path('grids') }}" class="btn btn-lg btn-primary mb-3 me-md-3">Sélectionner votre première grille</a>
						</div>
					{% else %}
						<div class="d-flex justify-content-between mt-5">
							<a href="{{ path('game') }}" class="btn btn-lg btn-primary mb-3 me-md-3">Continuer la partie en cours</a>
							<a href="{{ path('grids') }}" class="btn btn-lg btn-outline-secondary  mb-3">Changer de Grille</a>
						</div>
					{% endif %}
				{% else %}
					<div class="d-flex justify-content-center mt-5">
						<a href="{{ path('app_login') }}" class="btn btn-lg btn-primary mb-3">Connexion</a>
					</div>
				{% endif %}
				</div>
				<div id="home-rigth-section" class="col-lg-6 col-md-12 home-sections">
					<div class="container-home-rigth-section">
						{% if app.user %}
							<div id="home-ranking">
								<h2 class="mb-4 fs_225 text-center">Classement</h2>
								<table class="table table-borderless home">
									<tbody>
										{% for user in ranking %}
											<tr>
												<td class="text-center">{{ user.username }}</td>
												<td class="text-center">{{ user.score }} points</td>
											</tr>
										{% endfor %}
									</tbody>
								</table>
							</div>
						{% else %}
							<div id="home-register">
								<h2 class="text-center">Inscription</h2>
								{{ form_start(form, {'action': path('app_register'), 'method': 'POST'}) }}
								{{ form_widget(form) }}
								<div class="d-flex justify-content-center mt-3">
									<button type="submit" class="btn btn-secondary">Créer un compte</button>
								</div>
								{{ form_end(form) }}
							</div>
						{% endif %}
					</div>
				</div>
			</div>

	</div>
{% endblock %}
